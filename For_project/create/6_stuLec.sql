// 각 학생별 하나의 테이블
-- Create a new relational table with 3 columns

CREATE TABLE LEC_INT 
(
      STU_ID VARCHAR2(10),
      LEC_ID VARCHAR2(6) NOT NULL,  //FOREIGN
      LEC_NAME //FOREIGN
      LEC_DIV //FOREIGN
      LEC_COM //FOREIGN
      LEC_CRE //FOREIGN
      LEC_PRO //FOREIGN
      LEC_ROOM //FOREIGN
      LEC_CUR //FOREIGN
      LEC_LMT //FOREIGN
);

COMMIT;